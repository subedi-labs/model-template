name: Run SAST

on:
  pull_request:
    branches: [ "main" ]

jobs:
  codeql:
    uses: subedi-labs/platform/.github/workflows/scan-codeql.yml@main
    permissions:
      contents: read
      security-events: write
    with:
      # Empty unless custom queries needed
      config_file: ""
